declare module '@emotion/react' {
  export interface Theme {
    colors: {
      primary: string;
      hover: string;
    };
  }
}

export const theme = {
  colors: {
    primary: 'black',
    hover: '#6aa84f',
  },
};
